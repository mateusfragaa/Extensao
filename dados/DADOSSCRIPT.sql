-- MySQL Script generated by MySQL Workbench
-- Sun Mar 29 07:47:18 2026
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Table `TB_PAGAMENTO`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_PAGAMENTO` ;

CREATE TABLE IF NOT EXISTS `TB_PAGAMENTO` (
  `PAG_ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `PAG_VALOR` DECIMAL(10,2) UNSIGNED NOT NULL,
  `PAG_CREATED_AT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `PAG_STATUS` CHAR(1) NOT NULL DEFAULT 'A' COMMENT 'A= ABERTO; P =PAGO; C = CANCELADO',
  `PAG_VALOR_PAGO` DECIMAL(10,2) NULL DEFAULT 0,
  `PAG_VALOR_ABERTO` DECIMAL(10,2) GENERATED ALWAYS AS (PAG_VALOR) VIRTUAL,
  `PAG_FAVORECIDO_ID` INT UNSIGNED NOT NULL,
  `PAG_OBSERVACAO` TEXT NULL,
  `PAG_DATA_BAIXA` DATETIME NULL,
  PRIMARY KEY (`PAG_ID`),
  UNIQUE INDEX `REC_ID_UNIQUE` (`PAG_ID` ASC) VISIBLE,
  INDEX `PAG_FAVORECIDO_idx` (`PAG_FAVORECIDO_ID` ASC) VISIBLE,
  CONSTRAINT `PAG_FAVORECIDO`
    FOREIGN KEY (`PAG_FAVORECIDO_ID`)
    REFERENCES `TB_PESSOA` (`PES_ID`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci
ROW_FORMAT = DYNAMIC;


-- -----------------------------------------------------
-- Table `TB_PAGAMENTO_ITEM`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_PAGAMENTO_ITEM` ;

CREATE TABLE IF NOT EXISTS `TB_PAGAMENTO_ITEM` (
  `PAGI_ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `PAGI_PAG_ID` INT UNSIGNED NOT NULL,
  `PAGI_VALOR` DECIMAL(10,2) UNSIGNED NOT NULL,
  `PAGI_CREATED_AT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `PAGI_TIPO_DOCUMENTO` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`PAGI_ID`),
  UNIQUE INDEX `RECI_ID_UNIQUE` (`PAGI_ID` ASC) VISIBLE,
  INDEX `PAGI_TIPO_DOCUMENTO_idx` (`PAGI_TIPO_DOCUMENTO` ASC) VISIBLE,
  INDEX `PAGI_PAG_ID_idx` (`PAGI_PAG_ID` ASC) VISIBLE,
  CONSTRAINT `PAGI_TIPO_DOCUMENTO`
    FOREIGN KEY (`PAGI_TIPO_DOCUMENTO`)
    REFERENCES `TB_TIPO_DOCUMENTO` (`TDC_ID`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `PAGI_PAG_ID`
    FOREIGN KEY (`PAGI_PAG_ID`)
    REFERENCES `TB_PAGAMENTO` (`PAG_ID`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_PEDIDO_VENDA`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_PEDIDO_VENDA` ;

CREATE TABLE IF NOT EXISTS `TB_PEDIDO_VENDA` (
  `PEV_ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `PEV_DATA_VENDA` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
  `PEV_TOTAL` DECIMAL(10,2) UNSIGNED NOT NULL DEFAULT 0,
  `PEV_STATUS` CHAR(1) NOT NULL DEFAULT 'A' COMMENT 'A = ABERTO; F = FATURADA; C = CANCELADA',
  `PEV_ACRESCIMO` DECIMAL(10,2) UNSIGNED NULL DEFAULT 0,
  `PEV_DESCONTO` DECIMAL(10,2) UNSIGNED NULL DEFAULT 0,
  `PEV_CLIENTE_ID` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`PEV_ID`),
  UNIQUE INDEX `PEV_ID_UNIQUE` (`PEV_ID` ASC) VISIBLE,
  INDEX `PEV_CLIENTE_ID_idx` (`PEV_CLIENTE_ID` ASC) VISIBLE,
  CONSTRAINT `PEV_CLIENTE_ID`
    FOREIGN KEY (`PEV_CLIENTE_ID`)
    REFERENCES `TB_PESSOA` (`PES_ID`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_PEDIDO_VENDA_ITEM`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_PEDIDO_VENDA_ITEM` ;

CREATE TABLE IF NOT EXISTS `TB_PEDIDO_VENDA_ITEM` (
  `PEVI_ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `PEVI_VENDA_ID` INT UNSIGNED NOT NULL,
  `PEVI_PRD_ID` INT UNSIGNED NOT NULL,
  `PEVI_QUANTIDADE` DECIMAL(10,2) UNSIGNED NOT NULL,
  `PEVI_PRECO_UNITARIO` DECIMAL(10,2) UNSIGNED NOT NULL,
  `PEVI_SUBTOTAL` DECIMAL(10,2) GENERATED ALWAYS AS (PEVI_QUANTIDADE * PEVI_PRECO_UNITARIO) VIRTUAL,
  PRIMARY KEY (`PEVI_ID`),
  UNIQUE INDEX `PEVI_ID_UNIQUE` (`PEVI_ID` ASC) VISIBLE,
  INDEX `PEVI_VENDA_ID_idx` (`PEVI_VENDA_ID` ASC) VISIBLE,
  INDEX `PEVI_PRD_ID_idx` (`PEVI_PRD_ID` ASC) VISIBLE,
  CONSTRAINT `PEVI_VENDA_ID`
    FOREIGN KEY (`PEVI_VENDA_ID`)
    REFERENCES `TB_PEDIDO_VENDA` (`PEV_ID`)
    ON DELETE CASCADE
    ON UPDATE RESTRICT,
  CONSTRAINT `PEVI_PRD_ID`
    FOREIGN KEY (`PEVI_PRD_ID`)
    REFERENCES `TB_PRODUTO` (`PRD_ID`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_PESSOA`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_PESSOA` ;

CREATE TABLE IF NOT EXISTS `TB_PESSOA` (
  `PES_ID` INT UNSIGNED NOT NULL,
  `PES_NOME` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`PES_ID`),
  UNIQUE INDEX `CLI_ID_UNIQUE` (`PES_ID` ASC) VISIBLE,
  UNIQUE INDEX `CLI_NOME_UNIQUE` (`PES_NOME` ASC) VISIBLE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_PRODUTO`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_PRODUTO` ;

CREATE TABLE IF NOT EXISTS `TB_PRODUTO` (
  `PRD_ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `PRD_DESCRICAO` VARCHAR(45) NOT NULL,
  `PRD_QUANTIDADE` DECIMAL(10,3) UNSIGNED NOT NULL DEFAULT 0,
  `PRD_STATUS` TINYINT(1) NOT NULL DEFAULT 1 COMMENT '1 = ATIVO; 0 = INATIVO',
  `PRD_PRECO_VENDA` DECIMAL(10,2) UNSIGNED NULL DEFAULT 0,
  `PRD_PRECO_CUSTO` DECIMAL(10,2) UNSIGNED NULL DEFAULT 0,
  `PRD_UNIDADE` VARCHAR(10) NOT NULL DEFAULT 'UN',
  `PRD_CREATED_AT` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
  `PRD_GRUPO` VARCHAR(45) NULL,
  PRIMARY KEY (`PRD_ID`),
  UNIQUE INDEX `PRD_DESCRICAO_UNIQUE` (`PRD_DESCRICAO` ASC) VISIBLE,
  UNIQUE INDEX `PRD_ID_UNIQUE` (`PRD_ID` ASC) VISIBLE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_RECEBIMENTO`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_RECEBIMENTO` ;

CREATE TABLE IF NOT EXISTS `TB_RECEBIMENTO` (
  `REC_ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `REC_VENDA_ID` INT UNSIGNED NOT NULL,
  `REC_VALOR` DECIMAL(10,2) UNSIGNED NOT NULL,
  `REC_CREATED_AT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `REC_STATUS` CHAR(1) NOT NULL DEFAULT 'A' COMMENT 'A= ABERTO; B = BAIXADO; C = CANCELADO',
  `REC_VALOR_PAGO` DECIMAL(10,2) NULL DEFAULT 0,
  `REC_VALOR_ABERTO` DECIMAL(10,2) GENERATED ALWAYS AS (REC_VALOR) VIRTUAL,
  `REC_OBSERVACAO` TEXT NULL,
  `REC_DEVEDOR_ID` INT UNSIGNED NOT NULL,
  `REC_DATA_BAIXA` DATETIME NULL,
  PRIMARY KEY (`REC_ID`),
  UNIQUE INDEX `REC_ID_UNIQUE` (`REC_ID` ASC) VISIBLE,
  INDEX `REC_VENDA_ID_idx` (`REC_VENDA_ID` ASC) VISIBLE,
  INDEX `REC_DEVEDOR_ID_idx` (`REC_DEVEDOR_ID` ASC) VISIBLE,
  CONSTRAINT `REC_VENDA_ID`
    FOREIGN KEY (`REC_VENDA_ID`)
    REFERENCES `TB_PEDIDO_VENDA` (`PEV_ID`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `REC_DEVEDOR_ID`
    FOREIGN KEY (`REC_DEVEDOR_ID`)
    REFERENCES `TB_PESSOA` (`PES_ID`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_RECEBIMENTO_ITEM`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_RECEBIMENTO_ITEM` ;

CREATE TABLE IF NOT EXISTS `TB_RECEBIMENTO_ITEM` (
  `RECI_ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `RECI_REC_ID` INT UNSIGNED NOT NULL,
  `RECI_VALOR` DECIMAL(10,2) UNSIGNED NOT NULL,
  `RECI_CREATED_AT` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
  `RECI_TIPO_DOCUMENTO` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`RECI_ID`),
  UNIQUE INDEX `RECI_ID_UNIQUE` (`RECI_ID` ASC) VISIBLE,
  INDEX `RECI_REC_ID_idx` (`RECI_REC_ID` ASC) VISIBLE,
  INDEX `RECI_TIPO_DOCUMENTO_idx` (`RECI_TIPO_DOCUMENTO` ASC) VISIBLE,
  CONSTRAINT `RECI_REC_ID`
    FOREIGN KEY (`RECI_REC_ID`)
    REFERENCES `TB_RECEBIMENTO` (`REC_ID`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE,
  CONSTRAINT `RECI_TIPO_DOCUMENTO`
    FOREIGN KEY (`RECI_TIPO_DOCUMENTO`)
    REFERENCES `TB_TIPO_DOCUMENTO` (`TDC_ID`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_TIPO_DOCUMENTO`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_TIPO_DOCUMENTO` ;

CREATE TABLE IF NOT EXISTS `TB_TIPO_DOCUMENTO` (
  `TDC_ID` INT UNSIGNED NOT NULL,
  `TDC_DESCRICAO` VARCHAR(45) NOT NULL,
  `TDC_STATUS` TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '1 = ATIVO; 0 = INATIVO',
  PRIMARY KEY (`TDC_ID`),
  UNIQUE INDEX `TDC_ID_UNIQUE` (`TDC_ID` ASC) VISIBLE,
  UNIQUE INDEX `TDC_DESCRICAO_UNIQUE` (`TDC_DESCRICAO` ASC) VISIBLE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


-- -----------------------------------------------------
-- Table `TB_USUARIO`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `TB_USUARIO` ;

CREATE TABLE IF NOT EXISTS `TB_USUARIO` (
  `USU_ID` INT UNSIGNED NOT NULL,
  `USU_NOME` VARCHAR(45) NOT NULL,
  `USU_SENHA` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`USU_ID`),
  UNIQUE INDEX `USU_ID_UNIQUE` (`USU_ID` ASC) VISIBLE,
  UNIQUE INDEX `USU_NOME_UNIQUE` (`USU_NOME` ASC) VISIBLE,
  UNIQUE INDEX `USU_SENHA_UNIQUE` (`USU_SENHA` ASC) VISIBLE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb4
COLLATE = utf8mb4_unicode_ci;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
